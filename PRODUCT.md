# VRP-0 产品原则

## 用户与目的

VRP-0 面向需要规划、核对和解释现场服务排程的调度人员、运营人员与工程人员。产品把一个当前场景转化为可审阅的路线和时间安排，并通过 REST、MCP 与静态控制台操作同一份当前场景和任务数据。

结果界面首先服务于“看清计划、发现缺失、可信交接”，不是实时人员跟踪、地图导航、业务工单编辑或聊天代理自主决策。MCP Apps 是只读结果界面：Map 负责空间路线与规划回放，Gantt 负责时间顺序与对象核对。

## 体验原则

### 精准

* 只展示可由当前契约和归档事实支持的数据，不补造位置、路线、时间、归属、状态或计数。
* 区分未知、空、失败、不可播放和未授权；不能用零值或通用成功文案掩盖差异。
* 业务计划时间不随浏览器时区改写；路线、工单和工程师顺序保持原始身份与段位。

### 克制

* 每个界面只保留当前任务所需的主视图和少量明确动作。Map inline 不重复任务摘要或对象详情；Gantt 不加载地图能力。
* 只读界面不引入创建、求解、停止、删除或场景写入动作，不因文档空白增加产品能力。
* 外部网络、权限和宿主能力默认最小化；失败时明确降级，不以自动重试或扩大 CSP 隐藏问题。

### 可信

* 明确标示规划结果与规划回放，不称为实时位置或实际执行轨迹。
* 任何跨资源、刷新或全屏行为都需要用户显式触发，并保持任务、版本和工具身份可验证。
* 模拟测试、构建成功和候选 CSP 不等于生产可用；真实导入、地图网络和宿主联调必须单独记录。

## 可访问性

* 跟随宿主中英文、浅深主题和可用尺寸，窄屏仍可到达主要操作与内容。
* 使用语义控件、可见键盘焦点和读屏名称；状态、选中、告警和阶段不能只依赖颜色。
* 支持减少动态效果；播放默认暂停，在页面隐藏、退出全屏、权限失效或销毁时停止。
* 不可信名称、地址和 ID 始终作为纯文本显示；长 ID 保留完整值，不用截断文本进行身份匹配。

具体视觉规则以 [`docs/ui/theme.md`](docs/ui/theme.md) 为事实源；功能、契约、测试和发布边界分别以 `docs/` 对应事实源为准。
