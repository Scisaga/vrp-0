# requirements

## 场景页
- 展示场景基础信息：场景名称、城市、开始时间、周期、场景描述。
- 支持在场景配置与求解任务之间切换，并展示求解任务数量。
- 支持保存场景。
- 支持删除场景。
- 支持规划求解，并设置求解时间。
- 展示当前场景的求解中任务、排队任务、最优解任务。
- 提供对比分析入口。
- 提供业务超参数优化入口。
- 提供地址解析入口。
- 提供在途矩阵生成入口。
- 展示仓库、车辆/工程师、工单、SKU、成本参数页签。

## 仓库
- 支持维护仓库列表。
- 支持编辑仓库字段：ID、名称、地址。
- 支持仓库地址输入自动补全提示。
- 支持根据地址提示项在地图中展示位置并高亮标记。
- 支持地址解析后生成地图定位结果。
- 支持表格离焦校验并高亮缺失内容单元格。
- 支持表格从 Excel 整体复制粘贴。

## 车辆 / 工程师
- 支持维护车辆/工程师列表。
- 支持编辑车辆/工程师字段：ID、日期、名称、初始地址、技能、核定载重、核定容积、车辆类型、燃料类型、油耗/电耗、是否租用、每日出车费、班次开始时间、班次结束时间。
- 支持初始地址优先从已录入仓库地址中选择。
- 支持技能使用标签输入。
- 支持按日期维护班次开始时间和班次结束时间。
- 展示车辆/工程师已分配工单列表。
- 支持悬停已分配工单查看摘要信息。
- 支持点击已分配工单跳转到工单编辑区域并定位目标行。
- 支持选中行后整行高亮。
- 支持快捷删除行。
- 支持表格自动补充空白行。

## 工单
- 支持维护工单列表。
- 支持编辑工单字段：ID、类型、客户位置、所需技能、上一级工单、下一级工单、总重、总体积、时间窗开始、时间窗结束、上门配送时长、分配车辆/工程师、到达时间。
- 支持适配持续规划场景，在现有规划方案基础上增减工单和 Agent。
- 支持所需技能使用标签输入。
- 支持上一级工单多选，并自动更新关联的下一级工单。
- 支持下一级工单多选，并自动更新关联的上一级工单。
- 支持手动指定分配车辆/工程师。
- 支持手动指定到达时间。
- 支持工单指派后同步更新对应车辆/工程师的已分配工单。

## SKU
- 支持维护 SKU 列表。
- 支持编辑 SKU 字段：ID、名称、重量、体积。

## 场景约束与求解
- 支持按约束场景配置求解参数。
- 提供默认约束场景：成本综合、时间优先、里程优先、均衡派单。
- 支持自定义约束场景名称。
- 支持维护约束项列表。
- 支持为约束项设置约束类型。
- 支持为约束项设置超约束惩罚值。
- 提交规划求解后展示任务提交成功提示。
- 支持从提交提示跳转到求解任务详情。
- 支持继续编辑当前场景。

## 求解任务详情
- 展示求解任务基础信息：状态、求解开始时间、预计求解结束时间、总车辆/工程师数、总订单数。
- 展示求解结果信息：是否存在可行解、目标函数、总里程、总配送时间、总成本预估、吨公里费预估。
- 展示求解过程曲线。
- 展示成本参数和场景约束。
- 支持刷新任务数据。
- 支持自动刷新任务数据。
- 支持手动停止求解中的任务。
- 支持删除已完成或已停止的任务。
- 支持将求解结果更新回场景，用于持续规划。
- 使用 Gantt 图展示车辆/工程师与工单指派结果。
- 支持点选行，在右侧查看配送图。
- 支持悬停工单块时在地图中高亮对应标记并展示工单信息。
- 支持悬停车辆/工程师行头时展示车辆/工程师信息。
- 支持从地图 Overlay 展开进入求解任务一张图。

## 求解任务一张图
- 提供求解任务一张图页面。
- 地图支持拖拽和缩放。
- 地图展示每辆车的路径、实时位置和订单信息。
- 不同车辆使用不同颜色区分。
- 支持模拟车辆实时位置。
- 展示当前模拟时间。
- 支持根据路径信息计算并展示车辆位置。
- 支持显示车辆标记和订单标记。
- 右侧工具栏展示所有车辆当前状态。
- 支持按车辆显示/隐藏路径及路径上的订单。
- 展示车辆当前任务和下一个任务。
- 支持折叠和展开车辆状态信息。
- 底部时间轴支持拖拽调整模拟时间。
- 支持自动播放模拟。
- 支持回退到当天开始时间点。
- 支持最适缩放并自动居中重置缩放层级。
- 支持折叠返回求解任务详情。

## 接口及限流设置
- 提供 `/quota` 配置页面。
- 支持维护地图接口私钥。
- 支持维护每秒请求数（QPS）。
- 支持维护请求排队超时。
- 支持维护配额。
- 支持维护配额统计周期。
- 支持保存配置。

## 页面关系
- `场景详情` 为父页面，进入单个场景后打开 `场景配置`。
- `场景配置` 使用一级 sheet 切换：`仓库`、`车辆/工程师`、`工单`、`SKU`、`成本参数`。
- `场景约束` 位于 `场景详情` 内，与场景基础信息、求解操作处于同一页面上下文。
- `求解任务` sheet 内展示当前场景下的任务状态、最优解、对比分析入口。
- 从 `场景详情` 的 `求解任务` sheet 选择具体任务后，进入 `求解任务详情`。
- `求解任务详情` 为 `场景详情` 下的子页面。
- `求解任务一张图` 为 `求解任务详情` 下的子页面。
- `求解任务详情` 通过地图 Overlay 的展开入口进入 `求解任务一张图`。
- `求解任务一张图` 通过折叠按钮返回 `求解任务详情`。
- `接口及限流设置` 为独立配置页面。
