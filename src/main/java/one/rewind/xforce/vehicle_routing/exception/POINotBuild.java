package one.rewind.xforce.vehicle_routing.exception;

public class POINotBuild extends Exception {
    public POINotBuild(){
        super();
    }

    public POINotBuild(String msg) {
        super(msg);
    }
}
